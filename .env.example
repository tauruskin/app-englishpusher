# Supabase — copy to .env.local and fill in (Project Settings → API in the Supabase dashboard).
# The anon key is public by design; Row Level Security is the actual security boundary.
# When these are missing, all account features silently degrade to guest mode.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
